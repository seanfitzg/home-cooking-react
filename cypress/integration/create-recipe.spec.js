import { it } from 'mocha';
import { url } from './config/testConfig.js';

describe('recipe tests', () => {
  it('should create a new recipe', () => {});

  it('should update a recipe', () => {});

  it('should list the new recipe', () => {});

  it('should delete a recipe', () => {});
});
